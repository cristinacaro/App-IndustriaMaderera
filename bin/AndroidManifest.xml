<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="industrias.rioitata"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="16" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        
       android:theme="@style/Widget.AppCompat.ListView.Menu">
        <activity
            android:name="LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="SecadoActivity"></activity>
        <activity android:name="CocinaActivity"></activity>
        <activity android:name="PrensaActivity"></activity>
        <activity android:name="PrincipalActivity"></activity>
    </application>

</manifest>
